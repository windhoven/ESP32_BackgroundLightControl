 namespace Daytimes {
  enum Daytime {
    DAY, // 0
    EVENING, // 1
    NIGHT,
    MORNING,
  };
};

 namespace DaytimeOverlay {
  enum Overlay {
    NONE, // 0
    THUNDER, // 1
  };
};

 namespace LightMode {
  enum Mode {
    DAYNIGHT, // 0
    PARTY,
    WORK
  };
};

namespace AudioGenre {
  enum GenreMode {
    GENRE_EDM,
    GENRE_ROCK,
    GENRE_AMBIENT,
     GENRE_HIPHOP,
  GENRE_TECHNO,
  GENRE_DNB,
  GENRE_CINEMATIC,
  GENRE_HOUSE,
    GENRE_COUNT
  };
};
